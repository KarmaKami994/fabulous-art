---
import BaseLayout from '@/layouts/BaseLayout.astro';
const locale = 'de';
const pageTitle = 'Über Mich';
---
<BaseLayout title={pageTitle} locale={locale}>
  <section class="section" style="padding-top: calc(var(--header-height) + var(--space-4xl));">
    <div class="container">
      <h1>{pageTitle}</h1>
      <p style="margin-top: var(--space-lg); color: var(--color-text-secondary);">Diese Seite wird noch aufgebaut.</p>
    </div>
  </section>
</BaseLayout>
