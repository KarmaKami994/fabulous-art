---
import BaseLayout from '@/layouts/BaseLayout.astro';
import CommissionWizards from '@/components/CommissionWizards.astro';
import { customDrawingTranslations } from '@/i18n/customDrawing';
import { commissionServiceJsonLd } from '@/lib/seo';

const t = customDrawingTranslations.de;
---
<BaseLayout title={t.pageTitle} locale="de" description={t.pageDesc} jsonLd={commissionServiceJsonLd('de')}>
  <CommissionWizards locale="de" />
</BaseLayout>
